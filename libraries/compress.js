

cmp = function() {

    // Radix
    this.radixConvert = true;
    this.radixNumber = 5; // Integers between 1 and 5 works best (2^5 = 32, radix convert only goes up to base 36)

    this.repeatShorten = true;

    this.compress = function(str) {

        
    }

    this.uncompress = function(str) {

    }
}